# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
playwright-report/
test-results/

# Next.js
.next/
out/
build/

# Production
dist/

# Misc
.DS_Store
*.pem
Thumbs.db

# Manual gerado (PDF e markdown consolidado sao build artefatos)
docs/manual/manual-servidor.pdf
docs/manual/manual-consolidado.md
docs/manual/.consolidado.md
docs/manual/dist/

# Placeholders PNG gerados automaticamente pelo script.
# Prints reais (capturados pelo user) devem ser commitados manualmente
# com git add -f docs/manual/images/NOME.png (forcando).
# O manifest rastreia quais arquivos sao placeholders vs reais.
docs/manual/images/*.png
docs/manual/images/.placeholders-manifest.json
!docs/manual/images/.gitkeep

# Estado de auth do Playwright (contem cookie de sessao — nunca commitar)
docs/manual/.auth-state.json

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files - NUNCA commitar secrets
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
.env.vercel
CREDENCIAIS.txt

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma
prisma/*.db
prisma/*.db-journal

# IDE
.idea/
*.swp
*.swo

# VS Code — ignora tudo, mas versiona os arquivos compartilhados do time
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# Configuracoes locais do Claude Code (pessoais do dev)
.claude/settings.local.json
.claude/settings.json
.mcp.json

# Arquivos pessoais/locais do dev (nao relacionados ao projeto)
Untitled
image.png
docs/ata modelo*.pdf

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Uploads (arquivos enviados por usuarios)
public/uploads/*
!public/uploads/.gitkeep
# Excecao: brasao/logo institucional sao assets fixos (versionados)
!public/uploads/logos/
!public/uploads/logos/*

# Backup do site antigo (dump SQL + CSVs com dados pessoais - NUNCA commitar)
docs/backup antigo/

# Cache
.cache/
.turbo/

# Sentry
.sentryclirc
