# =============================================================================
# Environment Variables Template - Produção
# Sistema de Câmara Municipal
# =============================================================================
#
# Variáveis a serem substituídas:
#   {{DATABASE_URL}}      - URL de conexão do banco de dados
#   {{DIRECT_URL}}        - URL direta do banco (para migrations)
#   {{DOMAIN}}            - Domínio do site
#   {{SITE_NAME}}         - Nome da Câmara Municipal
#   {{NEXTAUTH_SECRET}}   - Chave secreta para autenticação
#
# =============================================================================

# =============================================================================
# Banco de Dados
# =============================================================================
DATABASE_URL="{{DATABASE_URL}}"
DIRECT_URL="{{DIRECT_URL}}"

# =============================================================================
# Autenticação (NextAuth.js)
# =============================================================================
NEXTAUTH_URL="https://{{DOMAIN}}"
NEXTAUTH_SECRET="{{NEXTAUTH_SECRET}}"

# =============================================================================
# Identificação do Site
# =============================================================================
SITE_NAME="{{SITE_NAME}}"
SITE_URL="https://{{DOMAIN}}"
NEXT_PUBLIC_SITE_NAME="{{SITE_NAME}}"
NEXT_PUBLIC_SITE_URL="https://{{DOMAIN}}"

# =============================================================================
# Ambiente
# =============================================================================
NODE_ENV="production"

# =============================================================================
# Email (configurar conforme necessário)
# =============================================================================
# EMAIL_SERVER_HOST="smtp.gmail.com"
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=""
# EMAIL_SERVER_PASSWORD=""
# EMAIL_FROM="noreply@{{DOMAIN}}"

# =============================================================================
# Uploads
# =============================================================================
UPLOAD_DIR="./public/uploads"
MAX_FILE_SIZE=10485760

# =============================================================================
# Analytics (opcional)
# =============================================================================
# NEXT_PUBLIC_GA_ID=""

# =============================================================================
# Redis - Rate Limiting e Cache (RECOMENDADO para produção)
# =============================================================================
# Habilite Redis para rate limiting compartilhado entre instâncias
# Em produção com múltiplas réplicas, isso garante que o rate limit
# de login seja compartilhado entre todas as instâncias

# URL de conexão do Redis (usar nome do serviço Docker ou IP)
REDIS_URL="redis://redis:6379"

# Habilitar Redis (defina como "true" para usar Redis)
REDIS_ENABLED="true"

# =============================================================================
# Segurança - API Interna
# =============================================================================
# Chave secreta para validar chamadas internas (rate-limit, etc)
# IMPORTANTE: Gere uma chave única para cada ambiente!
# Gere com: openssl rand -base64 32
INTERNAL_API_SECRET="{{INTERNAL_API_SECRET}}"

# =============================================================================
# Observabilidade (opcional)
# =============================================================================
# SENTRY_DSN=""
# SENTRY_AUTH_TOKEN=""
